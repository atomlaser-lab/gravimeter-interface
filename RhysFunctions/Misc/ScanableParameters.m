classdef ScanableParameters < handle
    properties(Constant)
        Voltage = 'Voltage (V)';
        TOF = 'TOF (ms)';
        PulseDuration = '\tau (us)';
        TwoPhoton = '\delta (kHz)';        
        Power = 'Power (arb)';
        Run = 'Run Number (Arb)'
        
        
    end

    methods(Static)

    end
end